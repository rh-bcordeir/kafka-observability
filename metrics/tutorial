fazer o deploy das métricas de jmx-exporter. exemplos no github

criar configmap com as metricas 

na documentação tem a explicação. exporta metricas no kafka:

    metricsConfig:  1 
      type: jmxPrometheusExporter
      valueFrom:
        configMapKeyRef:
          name: kafka-metrics
          key: kafka-metrics-config.yml
         
adicionar o kafka exporter no cluster kafka

criar pod monitor kafka-resources-metrics

criar promtheus-rules

editar ou criar o cm cluser-monitoring-config e setar enableUserWorkload: true

criar sa para grafana e dar permissão de cluster-monitoring-view

criar a secret do SA com annotation que gera token

o grafana precisa de um data source do prometheus conectado ao thanos querier que se encontra no openshift-monitoring. Deve ser criado o datasource e criado um configmap dele

criar o deployment e service do grafana
criar rota

importar dashboards